apiVersion: v1
kind: Service
metadata:
  name: growth-simulator
  labels:
    {{- include "growth-simulator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "growth-simulator.selectorLabels" . | nindent 4 }}
