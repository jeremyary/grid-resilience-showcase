{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "growth-simulator.serviceAccountName" . }}
  labels:
    {{- include "growth-simulator.labels" . | nindent 4 }}
{{- end }}
